export function HealthPage(): JSX.Element {
  return (
    <main className="app-shell">
      <section className="app-panel" aria-labelledby="health-title">
        <p className="eyebrow">Virtual Coders CRM</p>
        <h1 id="health-title">Web health: ok</h1>
        <p>The frontend application is running.</p>
      </section>
    </main>
  );
}
